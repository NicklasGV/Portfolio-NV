# Copy this file to .env if you want to override the default Web3Forms access key
# used by the contact form (src/components/Contact.vue). Not required — the form
# already has a working key baked in as a fallback.
VITE_WEB3FORMS_ACCESS_KEY=your-web3forms-access-key
